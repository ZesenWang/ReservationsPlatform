<resources>
    <!-- Splash Screen 主题 -->
    <style name="AppSplashTheme" parent="@android:style/Theme.NoTitleBar">
        <item name="android:windowBackground">@drawable/splash_image</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="reserveactivity_fragment3_popupwindow">
        <item name="android:windowEnterAnimation">@anim/reserve_activity_fragment3_popupwindow_in</item>
        <item name="android:windowExitAnimation">@anim/reserve_activity_fragment3_popupwindow_out</item>
    </style>
</resources>
